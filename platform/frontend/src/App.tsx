/** Default catch-all — should not render in normal routing. */
export default function App() {
  return (
    <div className="p-8 text-center text-muted">
      <p>Page not found.</p>
    </div>
  );
}
